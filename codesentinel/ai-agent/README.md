# AI Agent

This module owns Azure OpenAI integration and structured prompts. Credentials
and model configuration will be read from environment variables in a later
phase; no secrets belong in the frontend or source code.