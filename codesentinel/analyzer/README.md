# Analyzer

The analyzer is intentionally a separate module. Phase 1 defines its boundary;
Python static analyzers for Python and JavaScript will be added in Phase 4.

The target output is the shared finding contract documented in
`../docs/api-contract.md`.